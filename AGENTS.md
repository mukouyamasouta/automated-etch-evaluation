# GaN手法Bトレース：Codex作業指示

このリポジトリは、野島氏のGaNエッチング形状自動評価における手法Bを、重み学習から追試するためのものである。

## 作業開始時

毎回、作業を始める前に次を行う。

1. `docs/PROJECT_STATE.md`を全文読む。
2. `git status --short --branch`と直近のコミットを確認する。
3. `README.md`を読む。ローカルで移動済みの場合は`docs/README.md`を読む。
4. ファイル選定が関係する場合は`docs/FILE_INVENTORY.md`を読む。
5. Datasetの意味が関係する場合は`docs/Dataset_ファイル構成ガイド.html`を参照する。

## 採用構成

- Detection: Faster R-CNN Det163
- Segmentation: U-Net Seg204
- End-To-End: Seg204 / Det163
- 処理順序: 全体SEMの物体検出 → 構造単位切出し → U-Net → 寸法計測・形状評価

採用ファイルの正確なパスと使用するDatasetは`docs/PROJECT_STATE.md`を正とする。

## 安全上の制約

- `/Volumes/met-info/Research Progress/Mukoyama/Model_backup`は原本であり、読み取り専用として扱う。変更・削除・整理をしない。
- Dataset、`.pth`、学習出力、`.7z`をGitへ追加しない。
- ユーザーが明示的に許可するまで、本設定による長時間学習やEnd-To-End推論を開始しない。
- ローカル動作確認と本実験を明確に区別する。縮小条件の結果を論文再現結果として扱わない。
- 既存の未コミット変更はユーザーの作業として扱い、無断で戻したりコミットへ混ぜたりしない。
- 外付けボリューム上の大量削除や上書きは、対象を読み取り確認してから行う。

## 実験時の記録

実験前に、使用コード、Gitコミット、Dataset、epoch、batch size、画像数、画像サイズ、deviceを確認する。実験後は`docs/EXPERIMENT_LOG.md`へ、日時、所要時間、結果、生成物、エラー、次の判断を追記する。

コードや設定を変更した場合は、変更理由と「ローカル確認用／本実験用」のどちらかを明記する。可能な限り元の基準コードを直接上書きせず、目的が分かる別ファイルまたは設定として管理する。

## Git運用

- 変更前後に`git diff`と`git status`を確認する。
- コード、設定、説明、実験ログだけをコミットする。
- 1コミットには一つの目的を持たせる。
- push前に、Dataset、重み、生成画像、個人情報、大容量ファイルが含まれていないことを確認する。

## Git初心者への教え方

ユーザーはGit初心者である。Git操作を伴う作業では、単にコマンドを実行するだけでなく、ユーザーの理解度に合わせて次を短く説明する。

- 今回Gitで記録した対象と、記録しなかった対象
- `git status`で現在状態を見る方法
- `git diff`またはGitHubで変更行を見る方法
- コミットが何を意味するか
- 安全に元へ戻す方法。未コミット変更には`git restore`、履歴を残して戻す場合は`git revert`を優先し、両者の違いを説明する
- 実行前に確認が必要な、変更を失う可能性のあるコマンド

Gitの説明は、その時点の実ファイルと実コミットを例にする。初心者に対して`reset --hard`、`clean -fd`、強制pushを安易に案内または実行しない。

## 現在の優先事項

`docs/PROJECT_STATE.md`の「次に行うこと」に従う。現在の想定は、Det163のローカル動作確認版を用意し、少数データ・1 epochで処理時間とメモリ負荷を測ることである。
